# HookDLL/CMakeLists.txt
# Sub-project file — included by root CMakeLists.txt via add_subdirectory if needed.
# Standalone build instructions are here for reference.

# All configuration is in the root CMakeLists.txt.
# This file is a placeholder for IDE navigation.
message(STATUS "HookDLL sub-directory processed (config in root CMakeLists.txt)")
